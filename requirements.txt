# =============================================================================
#  Agentic AI System for Automated Hospital Discharge Summaries
#  Core runtime requirements  (Python 3.11+ / tested on 3.12)
# =============================================================================
#   pip install -r requirements.txt
#
#  Heavy / optional extras (local sentence-transformer embeddings, OCR) live in
#  requirements-optional.txt so the core stack installs quickly.
# =============================================================================

# --- Configuration / utilities ----------------------------------------------
python-dotenv>=1.0.1
PyYAML>=6.0.1
httpx>=0.27.0
pydantic>=2.8.0
tenacity>=8.3.0

# --- LLM gateway & provider --------------------------------------------------
# Amazon Bedrock is the ONLY provider (see src/discharge_ai/llm/provider.py):
# Amazon Nova Lite by default, Cohere Command R+ as the "fast" model, both over
# the unified Converse API. litellm is what the ADK agents use to reach Bedrock.
langchain-core>=0.3.0
langchain-aws>=0.2.0
boto3>=1.35.0
litellm>=1.60.0

# --- Agent frameworks --------------------------------------------------------
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=2.0.0
google-adk>=1.0.0
agno>=1.0.0
# Agno's SqliteDb session store (RAG agent, last-3-turns context) needs this
SQLAlchemy>=2.0.30

# --- MCP (Model Context Protocol) -------------------------------------------
# Pinned to the 1.x line on purpose:
#   * `mcp.server.fastmcp.FastMCP` (the spec's server API) was removed in 2.0
#   * both google-adk and agno declare `mcp>=1.24,<2`
mcp>=1.24,<2

# --- A2A (Agent-to-Agent) Protocol ------------------------------------------
# Pinned to the 0.2.x line on purpose: it is the generation whose API the
# specification names directly (`A2AStarletteApplication`, `send_message()`,
# `send_message_streaming()`, AgentCard served at /.well-known/agent.json).
# a2a-sdk >= 1.0 replaced those pydantic types with protobuf-generated ones and
# moved the card to /.well-known/agent-card.json.
a2a-sdk>=0.2.16,<0.3

# --- Vector store / RAG ------------------------------------------------------
faiss-cpu>=1.8.0
numpy>=1.26.0

# --- Observability -----------------------------------------------------------
langfuse>=2.50.0

# --- Document parsing --------------------------------------------------------
pdfplumber>=0.11.0
pypdf>=4.2.0
python-docx>=1.1.2
Pillow>=10.3.0

# --- Reporting ---------------------------------------------------------------
Jinja2>=3.1.4
reportlab>=4.2.0

# --- Mock EHR REST API -------------------------------------------------------
fastapi>=0.115.0
uvicorn>=0.30.0

# --- User interfaces ---------------------------------------------------------
# The HITL console needs st.navigation/st.Page for its gated flow, and the
# `width="stretch"` layout API the views already use throughout.
streamlit>=1.43.0
pandas>=2.2.0
gradio>=4.44.0
